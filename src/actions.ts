export const sendContact = (formData: FormData) => {
    // Aqui você pode adicionar lógica para enviar os dados do formulário
    console.log('Form data received:', formData);
    return 'sucesso';
};